# Copy to secret.yaml, fill in, and apply OUT OF BAND (it is gitignored):
#   kubectl -n rk-terminauto apply -f k8s/secret.yaml
#
# Do NOT commit real secrets to a GitOps repo. If you run sealed-secrets or
# external-secrets, generate the encrypted form instead and commit THAT.
apiVersion: v1
kind: Secret
metadata:
  name: rk-terminauto-secrets
  namespace: rk-terminauto-secrets
type: Opaque
stringData:
  TWOCAPTCHA_API_KEY: "your_2captcha_key_here"
  NTFY_TOPIC: "rk-termin-losa-47493de1"
